NOTES:
1. The Zip file contains "CNN", "Database_processing_code","KNN" and "Random_forest".
2. The "CNN" folder contains the code used to train and test the network. 
3. "Database_processing_code" contains the python code used to generate the three different normalized data. It also contains the code used generate "sorted tags".
4. "KNN" contains the code used to train the K-Nearest Neighbours as a baseline.
5. "Random_forest" contains the code to train the Random Forest as a baseline.
6. We have not included the raw or the processed data with the submission.
7. We have also not attached the trained model parameters with the submission.